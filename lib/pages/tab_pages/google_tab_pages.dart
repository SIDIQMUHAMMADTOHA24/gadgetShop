import 'package:flutter/material.dart';

class GoogleTabPages extends StatelessWidget {
  const GoogleTabPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Google');
  }
}
